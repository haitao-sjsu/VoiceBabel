# 2026-04-14 Development Log / 开发日志

### 1. Documentation Repository Overhaul / 文档仓库整理

- Added all research docs, plans, code reviews, and devlog to git repository (10,000+ lines)
- 将所有研究文档、计划、代码评审和开发日志加入 git 仓库（10,000+ 行）

- Restructured directory naming: `devlog/` → `.human-devlog/`, `.learn/` → `.human-learn/`
- 重命名目录结构：`devlog/` → `.human-devlog/`，`.learn/` → `.human-learn/`

- Created distributed CLAUDE.md files for Audio/, Config/, Services/, UI/, Utilities/ subdirectories
- 为各子目录创建分布式 CLAUDE.md 文件

### 2. Technical Research Expansion / 技术调研扩展

- Reorganized research docs with sequential numbering (removed robustness_analysis, renumbered 07-14)
- 重新整理研究文档编号（移除 robustness_analysis，重新编号 07-14）

- Added 13 new research documents (15-27): Claude Code worktree, Apple native speech, Deepgram API, ElevenLabs STT, Soniox API, Mistral speech, Speechmatics API, Groq speech, speech recognition community overview, Qwen3 ASR, local translation, commercial translation API, text enhancement features
- 新增 13 篇技术研究文档（15-27）：涵盖 Claude Code worktree、Apple 原生语音、Deepgram/ElevenLabs/Soniox/Mistral/Speechmatics/Groq 等语音 API、语音识别社区概览、Qwen3 ASR、本地翻译、商业翻译 API、文本增强功能

### 3. Code Reviews & Implementation Plans / 代码评审与实施计划

- Added 3 new code reviews: translation review, text cleanup review, robustness review
- 新增 3 篇代码评审：翻译评审、文本清理评审、健壮性评审

- Added 3 new implementation plans: priority fallback, Apple Translation, translation language pack
- 新增 3 篇实施计划：优先级回退、Apple 翻译、翻译语言包

- Renamed plan files to numbered convention (04-06)
- 将计划文件统一为编号命名（04-06）

### 4. Apple Translation Integration / Apple 翻译集成

- Created ServiceAppleTranslation.swift — local on-device translation using Apple Translation framework (macOS 15.0+)
- 新建 ServiceAppleTranslation.swift — 使用 Apple Translation 框架实现本地设备端翻译（macOS 15.0+）

- Added `translationEngine` setting to EngineeringOptions ("auto"/"apple"/"cloud") for translation strategy control
- 在 EngineeringOptions 新增 `translationEngine` 设置（"auto"/"apple"/"cloud"）控制翻译策略

- Implemented two-step translation in RecordingController: transcribe first (any API mode), then translate via Apple/Cloud
- 在 RecordingController 实现两步翻译流程：先转录（任意 API 模式），再通过 Apple/Cloud 翻译

- Updated StatusBarController menu to show translation engine status
- 更新菜单栏控制器，显示翻译引擎状态

- Fixed SwiftUI bridge and language pack availability detection in ServiceAppleTranslation
- 修复 ServiceAppleTranslation 中 SwiftUI 桥接和语言包可用性检测问题

- Updated CLAUDE.md and subdirectory docs to reflect new architecture
- 更新 CLAUDE.md 及子目录文档以反映新架构

### 5. Unit Tests / 单元测试

- Added unit tests for 5 core components: AudioEncoder, RecordingController, ServiceAppleTranslation, ServiceCloudOpenAI, ServiceLocalWhisper (634 lines)
- 为 5 个核心组件新增单元测试（634 行）

### 6. Priority-Based Fallback System / 优先级回退系统

- Implemented priority-based fallback for transcription engines (e.g., Cloud → Local → Realtime)
- 实现转录引擎优先级回退（如 Cloud → Local → Realtime）

- Implemented priority-based fallback for translation engines (e.g., Apple → Cloud)
- 实现翻译引擎优先级回退（如 Apple → Cloud）

- Added fallback configuration UI in Settings panel for both transcription and translation
- 在设置面板新增转录和翻译的回退配置 UI

- Added UserSettings defaults and SettingsStore persistence for fallback preferences
- 新增 UserSettings 默认值和 SettingsStore 持久化支持回退偏好

- Developed in `worktree-fallback-priority` branch, merged to main
- 在 `worktree-fallback-priority` 分支开发，合并至 main

### 7. CLAUDE.md Conventions / CLAUDE.md 规范

- Added git commit and push conventions section to CLAUDE.md (commit grouping, message style, Co-Authored-By)
- 在 CLAUDE.md 新增 git 提交与推送规范（提交分组、消息风格、Co-Authored-By）
