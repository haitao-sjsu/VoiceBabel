# 2026-04-13 Development Log / 开发日志

### 1. Git Repository Setup / 建立 Git 仓库

- Initialized git repository with proper .gitignore (Xcode, SPM, macOS, sensitive files)
- 初始化 git 仓库，配置 .gitignore（Xcode 构建产物、SPM、macOS 系统文件、敏感文件）

- Created GitHub private repository and pushed initial commit (55 files, 3 API modes)
- 创建 GitHub 私有仓库并推送初始提交（55 个文件，支持本地/网络/实时三种 API 模式）

- Fixed accidental commit of hidden working directories (.claude-research-tech, .claude-plan, etc.)
- 修复了误提交的隐藏工作目录

### 2. Makefile Build Automation / Makefile 构建自动化

- Created Makefile with targets: build, release, local, run, dev, clean, check, help
- 创建 Makefile，包含 build/release/local/run/dev/clean/check/help 等 target

- `make dev` replaces manual xcodebuild + quit + relaunch workflow
- `make dev` 替代了手动 xcodebuild + 退出 + 重启的工作流

- `make local` enables building without Apple ID (ad-hoc signing) for open-source contributors
- `make local` 支持无 Apple ID 编译（ad-hoc 签名），方便开源贡献者

- Updated CLAUDE.md to reference Makefile instead of raw xcodebuild commands
- 更新 CLAUDE.md，统一使用 Makefile 而非直接调用 xcodebuild

### 3. Merge Constants into EngineeringOptions / 合并配置文件

- Consolidated Constants.swift (25 fields) and EngineeringOptions.swift (15 fields) into a single file
- 将 Constants.swift（25 个字段）和 EngineeringOptions.swift（15 个字段）合并为单一文件

- Reorganized all 40 fields by pipeline data flow: API & Auth → Audio capture → Audio encoding → Transcription models → Network → Post-processing → Translation → Text output → Hotkey → Internal timing
- 按管线数据流重新分类全部 40 个字段

- Updated all 9 referencing files (Constants. → EngineeringOptions.)
- 更新了所有 9 个引用文件的命名空间

### 4. API Key Migration to macOS Keychain / API Key 迁移至 macOS Keychain

- Replaced hardcoded API key in source code with macOS Keychain secure storage
- 将源码中硬编码的 API Key 替换为 macOS Keychain 安全存储

- Created KeychainHelper.swift (save/load/delete/exists) and ApiKeyValidator.swift (GET /v1/models)
- 新建 KeychainHelper（增删查）和 ApiKeyValidator（通过 /v1/models 验证）

- Added API Key management section to Settings panel (input, validate, clear)
- 在设置面板新增 API 密钥管理区域（输入、验证、清除）

- Added Edit menu to Settings window to enable Cmd+V paste (LSUIElement app limitation)
- 为设置窗口添加 Edit 菜单以支持 Cmd+V 粘贴（菜单栏应用的限制）

- Services rebuild dynamically when API Key changes; auto-restores preferred API mode
- API Key 变更时动态重建服务并自动恢复用户偏好的 API 模式

- Removed apiKey from EngineeringOptions and deleted template file (no longer contains secrets)
- 从 EngineeringOptions 移除 apiKey，删除模板文件（不再包含敏感信息）

### 5. UI Internationalization (i18n) / UI 国际化

- Added LocaleManager for runtime locale management with instant switching (no restart needed)
- 新增 LocaleManager 实现运行时语言管理，即时切换无需重启

- Created Localizable.xcstrings with ~80 UI strings × 20 languages
- 创建 Localizable.xcstrings，包含约 80 条 UI 字符串的 20 种语言翻译

- Created LogStrings.xcstrings with ~110 log strings (en + zh)
- 创建 LogStrings.xcstrings，包含约 110 条日志字符串（英文 + 中文）

- Added Interface Language picker in Settings (Follow System + 20 languages with native names)
- 在设置面板新增界面语言选择器（跟随系统 + 20 种语言，以各自语言显示）

- Added "Same as Interface" option to Recognition Language picker
- 识别语言选择器新增「与界面语言一致」选项

- Expanded recognition language list from 5 to 20+ languages
- 识别语言从 5 种扩展到 20+ 种

- Added EngineeringOptions.logLanguage for bilingual log control (en/zh)
- 新增 EngineeringOptions.logLanguage 控制日志双语（英/中）

- Migrated all hardcoded Chinese strings across 16 files to localized English keys
- 将 16 个文件中所有硬编码中文字符串迁移为本地化英文 key
